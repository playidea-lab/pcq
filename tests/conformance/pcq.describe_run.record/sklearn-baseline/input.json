{
  "invocation": {
    "kind": "cli",
    "command": ["pcq", "describe-run", "FIXTURE_OUTPUT", "--json"]
  },
  "fixtures": {
    "FIXTURE_OUTPUT": "fixtures/output"
  },
  "schema_version": 1,
  "notes": "Static fixture (sklearn iris baseline, eval_acc=1.0) captured 2026-05-10. The describe-run command quotes the manifest verbatim so sha256 mismatches against the actual fixture files are tolerated; the live runner relies only on the matcher rules in spec/CONFORMANCE.md."
}
